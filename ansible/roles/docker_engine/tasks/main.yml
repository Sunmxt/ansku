---
- name: Install docker
  yum:
      name: docker
      state: latest
  notify:
    - enable docker engine
    - start docker engine

- name: Install docker-compose
  yum:
      name: docker-compose
      state: latest

- name: Install pipy docker-compose
  pip:
      name: docker-compose
