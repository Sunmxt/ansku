{
    "server" : "{{ server_name }}"
    , "server_port" : "{{ server_port }}"
    , "local_address" : "0.0.0.0"
    , "local_port": "5353"
    {% if mode == "tunnel" %}
    , "tunnel_address": "{{ tunnel_address }}:{{ tunnel_port }}"
    {% endif %}
    , "password" : "{{ encrypt_password }}"
    , "method" : "{{ encrypt_method }}"
    , "mode": "{{ protocol }}"
    , "timeout" : 600
}

